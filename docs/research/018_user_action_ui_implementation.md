---
date: 2026-01-15T16:00:00+09:00
researcher: Claude
topic: "User Action UI Implementation Analysis"
tags: [research, codebase, ui, chat, actions, visualization, interactive]
status: in-progress
---

# Research: 사용자 액션 UI 구현 가능성 분석

## Research Question
사용자가 수행할 수 있는 데이터 분석 액션들을 현재 채팅 UI 구조에서 어떻게 구현할 수 있는지, 특히 시각적 요소(표, 차트)와 인터랙티브 요소(선택지, 버튼)의 지원 현황과 확장 방안

## Summary

현재 Pluto Duck 시스템의 역량:
- **채팅 UI**: 마크다운 렌더링 완전 지원, 인터랙티브 선택지 UI 미지원
- **보드**: 차트/시각화 완전 지원 (recharts 기반), Send to Board 로직 미구현
- **파일 업로드**: 채팅창에서 미지원 (별도 데이터 소스 UI에서만 가능)

**핵심 Gap:**
1. 인터랙티브 선택지 UI (A 액션 UX)
2. 채팅 → 보드 전송 로직 (C 액션)
3. 채팅 파일 업로드 UI (D 액션)

---

## 1. 제안된 사용자 액션

### Action Map

```
A. Explore my data
   ├── A-1. Descriptive summary
   └── A-2. Topic discovery

B. Run analyses
   ├── B-1. Data cleaning
   └── B-2. Run analysis

C. Visualize results
   ├── C-1. Create report
   └── C-2. Create charts

D. Update my data (직접 실행)
```

### 공통 요구사항
- **Context 확인**: 모든 액션 시작 시 기존 context 확인 → 없으면 요청

---

## 2. 액션별 상세 Flow

### A-1. Descriptive Summary

**1단계: 데이터 개요 제시**
- 테이블별 샘플 3행 (표)
- 데이터 특성 요약 (행 수, 컬럼 수, 타입 등)

**2단계: 다음 액션 선택지 (4개)**

| 옵션 | 설명 | 이후 |
|------|------|------|
| 기본 통계 보기 | 평균, 분포, 결측치 등 + 시각화 | 계속 |
| 데이터 품질 점검 | 결측, 이상치, 중복, 타입 불일치 | 계속 |
| 특정 컬럼 자세히 보기 | 선택한 컬럼 상세 분포 | 계속 |
| 분석 방향 논의하기 | 이 데이터로 뭘 할 수 있을지 | → Topic discovery |

---

### A-2. Topic Discovery

**1단계: 방향 선택**

| 옵션 | 설명 |
|------|------|
| 함께 논의 | 사용자가 직접 탐색하며 가설 형성 |
| AI 제안 받기 | AI가 비즈니스 질문 5가지 제시 |

**Option 1: 함께 논의**
1. 상황 파악 (분석 이유, 궁금한 점)
2. 데이터 탐색 (테이블별 5행)
3. 관심 컬럼 선택
4. Descriptive stats + 시각화 + 해석
5. "해볼만한 게 생겼나요?"
   - Yes → 가설 명료화 → 승인 → 실행
   - No → AI 제안으로 전환

**Option 2: AI 제안**
1. 비즈니스 질문 5가지 + 가치 설명
2. 질문 선택
3. 필요 컬럼 + 데이터 충분성 판단
4. 명료화 → 실행

---

## 3. 현재 시스템 역량 분석

### 3.1 보유 도구 (Tools)

| 카테고리 | 도구 | 액션 지원 |
|----------|------|----------|
| **Schema** | `list_tables`, `describe_table`, `sample_rows` | A-1 ✅ |
| **Query** | `run_sql` | A, B ✅ |
| **Asset** | `save_analysis`, `run_analysis`, `list_analyses` 등 | B ✅ |
| **Source** | `list_sources`, `list_source_tables` | A ✅ |
| **시각화** | (채팅 도구 없음, 보드에서 렌더링) | C ⚠️ |
| **선택지 UI** | - | 전체 ❌ |
| **파일 업로드** | (채팅에 없음, 별도 UI) | D ❌ |

### 3.2 보유 스킬 (Skills)

| 스킬 | 설명 | 액션 매핑 |
|------|------|----------|
| `source-explorer` | 데이터 소스 탐색 | A-1 부분 |
| `sql-analysis` | SQL 분석 워크플로우 | B-2 |
| `data-lineage` | 데이터 의존성 추적 | - |
| `skill-creator` | 새 스킬 생성 | - |

---

## 4. 채팅 UI 렌더링 역량 분석

### 4.1 마크다운 지원 ✅ 완전 지원

**라이브러리**: Streamdown v1.4.0 + Shiki v3.14.0

| 요소 | 지원 | 위치 |
|------|------|------|
| 테이블 | ✅ | 마크다운 테이블 문법 |
| 불릿/리스트 | ✅ | 순서 있는/없는 |
| 코드블록 | ✅ | 문법 강조 (Shiki) |
| 헤더, 굵게, 링크 | ✅ | 기본 마크다운 |

**코드 위치**: [response.tsx](frontend/pluto_duck_frontend/components/ai-elements/response.tsx)

```typescript
import { Streamdown } from "streamdown";

export const Response = memo(
  ({ className, ...props }: ResponseProps) => (
    <Streamdown className={cn("size-full ...", className)} {...props} />
  )
);
```

### 4.2 인터랙티브 선택지 UI ❌ 미지원

**현재 구현된 특수 UI:**

| UI 패턴 | 용도 | 상태 |
|---------|------|------|
| Approval (승인) | 파일 수정 전 확인 | ✅ 구현됨 |
| Queue (체크리스트) | `write_todos` 결과 | ✅ 구현됨 |
| **선택지 버튼** | 다중 선택 제시 | ❌ 없음 |
| **라디오 버튼** | 단일 선택 | ❌ 없음 |
| **액션 버튼** | 클릭 가능한 액션 | ❌ 없음 |

**Approval UI 구조** (참고용):
```
┌─────────────────────────────────────────┐
│  🔒 파일 수정 승인 요청                   │
│  /workspace/data.sql                    │
│                                         │
│  [✓ 승인]  [✗ 거부]                      │
└─────────────────────────────────────────┘
```

**코드 위치**: [confirmation.tsx](frontend/pluto_duck_frontend/components/ai-elements/confirmation.tsx)

### 4.3 Tool 결과 렌더링

**기본 구조**: 접이식 패널 + JSON 코드블록

```
┌─ Run Sql ─────────────────────────────┐
│ ▶ SELECT * FROM users LIMIT 5        │ (접힘)
└───────────────────────────────────────┘

┌─ Run Sql ─────────────────────────────┐
│ ▼ SELECT * FROM users LIMIT 5        │ (펼침)
├───────────────────────────────────────┤
│ Input:                                │
│ ```json                               │
│ { "query": "SELECT..." }              │
│ ```                                   │
│ Output:                               │
│ ```json                               │
│ [{"id": 1, "name": "..."}, ...]       │
│ ```                                   │
└───────────────────────────────────────┘
```

**특수 처리**: `write_todos` → 체크리스트 UI로 변환

**코드 위치**: [ToolRenderer.tsx](frontend/pluto_duck_frontend/components/chat/renderers/ToolRenderer.tsx)

### 4.4 보드 시각화 역량 ✅ 완전 지원

**보드에서 이미 차트 렌더링이 가능합니다** (recharts 라이브러리 기반)

| 차트 타입 | 지원 | 고급 기능 |
|----------|------|----------|
| Bar Chart | ✅ | 수직/수평, 스택 |
| Line Chart | ✅ | 다중 라인 |
| Pie Chart | ✅ | - |
| Area Chart | ✅ | 스택 |
| Composed Chart | ✅ | 혼합 타입 |

**고급 기능:**
- **다중 Y축**: `yColumns` 배열로 여러 시리즈
- **그룹핑**: `groupByColumn`으로 카테고리별 분리
- **스택**: `stacked` 속성
- **듀얼 축**: `showDualAxis`로 좌/우 Y축

**코드 위치**: [AssetChartView.tsx](frontend/pluto_duck_frontend/components/editor/components/AssetChartView.tsx)

### 4.5 Send to Board 현황 ⚠️ UI만 구현

| 구성 요소 | 상태 |
|----------|------|
| UI 버튼 (ClipboardPlusIcon) | ✅ 있음 |
| 콜백 인터페이스 | ✅ 정의됨 |
| 보드 선택 모달 | ❌ 미구현 |
| Markdown → Lexical 변환 | ❌ 미구현 |
| 실제 전송 로직 | ❌ 미구현 |

**코드 위치**: [AssistantMessageRenderer.tsx:107-110](frontend/pluto_duck_frontend/components/chat/renderers/AssistantMessageRenderer.tsx#L107-L110)

### 4.6 파일 업로드 현황 ❌ 채팅에서 미지원

| 위치 | 파일 업로드 |
|------|------------|
| 채팅 Composer | ❌ 없음 (텍스트만) |
| 데이터 소스 UI | ✅ 있음 (CSV, Parquet 등) |

**코드 위치**: [Composer.tsx](frontend/pluto_duck_frontend/components/chat/Composer.tsx) - 파일 첨부 UI 없음

### 4.7 현재 렌더 아이템 타입 구조

```typescript
// types/chatRenderItem.ts
export type ChatRenderItem =
  | UserMessageItem           // 사용자 메시지
  | ReasoningItem             // 추론 프로세스
  | ToolItem                  // 도구 호출
  | AssistantMessageItem;     // AI 응답
  // 미지원: ActionChoiceItem, FileUploadRequestItem 등
```

---

## 5. Gap 분석

### 5.1 액션별 Gap

| 액션 | 데이터 처리 | 채팅 표시 | 보드 연동 | 전체 Gap |
|------|------------|----------|----------|---------|
| A-1 Descriptive | ✅ 가능 | ✅ 마크다운 | - | 저 (선택지 UX만) |
| A-2 Topic | ✅ 가능 | ✅ 마크다운 | - | 저 (선택지 UX만) |
| B-1 Cleaning | ✅ 가능 | ✅ 마크다운 | - | 저 |
| B-2 Analysis | ✅ 가능 | ✅ 마크다운 | - | 저 |
| C-1 Report | ✅ 가능 | ✅ 마크다운 | ⚠️ 로직 필요 | 중 |
| C-2 Charts | ✅ 보드에서 렌더링 | - | ⚠️ 로직 필요 | 중 |
| D Update | - | - | - | 중 (업로드 UI 필요) |

### 5.2 핵심 Gap 요약

1. **선택지 UI 부재** (A 액션 UX 개선)
   - 텍스트 기반 선택만 가능
   - 클릭 가능한 버튼/카드 없음

2. **채팅 → 보드 전송 로직 미구현** (C 액션)
   - 보드에서 차트 렌더링은 이미 가능 ✅
   - Send to Board 버튼 UI는 있음 ✅
   - **필요**: 보드 선택 모달 + Markdown → Lexical 변환 + 전송 로직

3. **채팅 파일 업로드 UI 부재** (D 액션)
   - 데이터 소스 UI에서는 업로드 가능 ✅
   - **필요**: 채팅 Composer에 파일 첨부 기능 또는 업로드 요청 UI

---

## 6. 구현 전략 제안

### 6.1 Phase 1: MVP (스킬 기반 텍스트)

**목표**: 기존 구조에서 바로 사용 가능한 형태

**새 스킬 4개 작성**:

| 스킬 | 액션 | 내용 |
|------|------|------|
| `data-explorer` | A-1, A-2 | 데이터 탐색 + 선택지 텍스트 제시 |
| `data-cleaner` | B-1 | 데이터 정제 워크플로우 |
| `report-builder` | C-1 | 텍스트 리포트 생성 |
| `data-updater` | D | 데이터 업데이트 가이드 |

**선택지 제시 방식** (마크다운):
```markdown
다음 중 원하는 분석을 선택해주세요:

1. **기본 통계 보기** - 평균, 분포, 결측치
2. **데이터 품질 점검** - 이상치, 중복 확인
3. **특정 컬럼 자세히 보기** - 상세 분포 분석
4. **분석 방향 논의하기** - 가능한 인사이트 탐색

번호로 선택하거나 직접 입력해주세요.
```

**장점**: 구현 없이 바로 적용 가능
**단점**: UX가 덜 직관적

---

### 6.2 Phase 2: UI 컴포넌트 개발

**목표**: 클릭 가능한 선택지 UI

#### 6.2.1 새 이벤트 타입 정의

```typescript
// Backend: events.py
class EventType(Enum):
    ACTION_CHOICE = "action_choice"  # 새로 추가

// Frontend: chatRenderItem.ts
interface ActionChoiceItem extends BaseRenderItem {
  type: 'action_choice';
  prompt: string;           // "다음 분석을 선택하세요"
  choices: {
    id: string;
    label: string;          // "기본 통계 보기"
    description?: string;   // "평균, 분포, 결측치 등"
    icon?: string;
  }[];
  multiSelect?: boolean;
  selectedIds?: string[];   // 선택된 항목
}
```

#### 6.2.2 새 렌더러 컴포넌트

```
frontend/pluto_duck_frontend/components/chat/renderers/
└── ActionChoiceRenderer.tsx  # 새로 생성
```

**UI 목업**:
```
┌─────────────────────────────────────────┐
│  📊 다음 분석을 선택하세요                 │
│                                         │
│  ┌─────────────────┐ ┌─────────────────┐│
│  │ 📈 기본 통계    │ │ 🔍 품질 점검    ││
│  │ 평균, 분포 등  │ │ 이상치, 중복   ││
│  └─────────────────┘ └─────────────────┘│
│  ┌─────────────────┐ ┌─────────────────┐│
│  │ 🎯 컬럼 상세    │ │ 💡 방향 논의    ││
│  │ 특정 컬럼 분석 │ │ 인사이트 탐색  ││
│  └─────────────────┘ └─────────────────┘│
└─────────────────────────────────────────┘
```

#### 6.2.3 구현 필요 파일

| 위치 | 파일 | 작업 |
|------|------|------|
| Backend | `events.py` | `ACTION_CHOICE` 이벤트 타입 추가 |
| Backend | 새 도구 or 미들웨어 | 선택지 이벤트 emit 로직 |
| Frontend | `chatRenderItem.ts` | `ActionChoiceItem` 타입 추가 |
| Frontend | `RenderItem.tsx` | 타입별 분기 추가 |
| Frontend | `ActionChoiceRenderer.tsx` | 새 컴포넌트 |
| Frontend | `useMultiTabChat.ts` | 선택 시 메시지 전송 핸들러 |

---

### 6.3 Phase 3: 채팅 → 보드 연동 (C 액션)

**목표**: 채팅에서 제안한 차트/리포트를 보드로 전송

#### 구현 Flow

```
┌─────────────────────────────────────────────────────────────┐
│ 채팅                                                         │
│ ┌─────────────────────────────────────────────────────────┐ │
│ │ AI: 월별 매출 추이를 보여드릴까요?                        │ │
│ │                                                          │ │
│ │ 차트 유형: Line Chart                                    │ │
│ │ X축: month                                               │ │
│ │ Y축: revenue, cost                                       │ │
│ │                                                          │ │
│ │ [📊 보드에서 보기]  [🔄 다른 차트로]                      │ │
│ └─────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────┘
          │
          │ 클릭
          ▼
┌─────────────────────────────────────────────────────────────┐
│ 보드 선택 모달                                               │
│ ┌─────────────────────────────────────────────────────────┐ │
│ │ 어떤 보드에 추가할까요?                                  │ │
│ │                                                          │ │
│ │ ○ 📋 분석 보드 1                                        │ │
│ │ ○ 📋 월별 리포트                                        │ │
│ │ ○ ➕ 새 보드 만들기                                     │ │
│ │                                                          │ │
│ │               [취소]  [추가]                              │ │
│ └─────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────┘
          │
          │ 추가
          ▼
┌─────────────────────────────────────────────────────────────┐
│ 보드 (렌더링)                                                │
│ ┌─────────────────────────────────────────────────────────┐ │
│ │ 📈 [차트 렌더링됨 - recharts AssetChartView]            │ │
│ └─────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────┘
```

#### 필요한 구현

| 파일 | 작업 |
|------|------|
| `SendToBoardModal.tsx` (신규) | 보드 선택 모달 컴포넌트 |
| `lexicalUtils.ts` (신규) | Markdown → Lexical JSON 변환 |
| `MultiTabChatPanel.tsx` | `onSendToBoard` 핸들러 연결 |
| `page.tsx` | 모달 상태 관리 + 보드 업데이트 로직 |

#### 차트 전송 시 데이터 구조

```typescript
// 채팅에서 제안하는 차트 스펙
interface ChartProposal {
  chartType: 'bar' | 'line' | 'pie' | 'area' | 'composed';
  xColumn: string;
  yColumns: string[];
  groupByColumn?: string;
  stacked?: boolean;
  title?: string;
  // 실제 데이터는 Asset ID 참조
  assetId: string;
}

// 보드로 전송 시 → AssetEmbed 노드로 변환
{
  type: "asset-embed",
  assetId: "...",
  viewType: "chart",
  chartConfig: { ... }
}
```

---

### 6.4 Phase 4: 파일 업로드 UI (D 액션)

**목표**: 채팅에서 파일 업로드 요청 시 UI 제공

#### Option A: Composer에 파일 첨부 버튼

```
┌─────────────────────────────────────────────────────────────┐
│ Composer                                                     │
│ ┌─────────────────────────────────────────────────────────┐ │
│ │ [📎] 데이터를 업데이트하고 싶어요                        │ │
│ └─────────────────────────────────────────────────────────┘ │
│                                        [Model ▼]  [전송]    │
└─────────────────────────────────────────────────────────────┘
```

**필요한 구현:**
- `Composer.tsx`에 파일 첨부 버튼 추가
- 파일 선택 → 업로드 API 호출 → 메시지에 첨부

#### Option B: AI가 업로드 UI 요청

```
┌─────────────────────────────────────────────────────────────┐
│ 채팅                                                         │
│ ┌─────────────────────────────────────────────────────────┐ │
│ │ AI: 데이터를 업로드해주세요                              │ │
│ │                                                          │ │
│ │ ┌─────────────────────────────────────┐                 │ │
│ │ │  📁 파일을 여기에 드래그하거나       │                 │ │
│ │ │     [파일 선택] 버튼을 클릭하세요    │                 │ │
│ │ │                                     │                 │ │
│ │ │  지원 형식: CSV, Parquet, JSON      │                 │ │
│ │ └─────────────────────────────────────┘                 │ │
│ └─────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────┘
```

**필요한 구현:**
- 새 이벤트 타입: `file_upload_request`
- 새 렌더러: `FileUploadRequestRenderer.tsx`
- 업로드 완료 → AI에게 결과 전달

---

## 7. 권장 로드맵

```
┌─────────────────────────────────────────────────────────────┐
│  Phase 1: MVP (스킬 기반)                                    │
│  ├── 스킬 4개 작성 (data-explorer, cleaner, report, update)│
│  ├── 텍스트 기반 선택지 패턴 정립                           │
│  └── 사용자 피드백 수집                                     │
├─────────────────────────────────────────────────────────────┤
│  Phase 2: 선택지 UI                                         │
│  ├── ActionChoiceItem 타입 정의                            │
│  ├── ActionChoiceRenderer 컴포넌트 개발                    │
│  ├── 백엔드 이벤트 emit 로직                               │
│  └── 선택 → 메시지 전송 핸들러                             │
├─────────────────────────────────────────────────────────────┤
│  Phase 3: 채팅 → 보드 연동 (C 액션)                         │
│  ├── SendToBoardModal 컴포넌트                             │
│  ├── Markdown → Lexical 변환 유틸                          │
│  ├── onSendToBoard 핸들러 연결                             │
│  └── 차트 스펙 → AssetEmbed 노드 변환                      │
├─────────────────────────────────────────────────────────────┤
│  Phase 4: 파일 업로드 UI (D 액션)                           │
│  ├── Composer 파일 첨부 버튼 또는                          │
│  ├── FileUploadRequest 이벤트 + 렌더러                     │
│  └── 업로드 → 백엔드 처리 → 결과 전달                      │
└─────────────────────────────────────────────────────────────┘
```

---

## 8. Open Questions

1. **D. Update my data 범위**:
   - 새 파일 업로드만?
   - 기존 테이블에 행 추가?
   - 기존 데이터 수정/삭제?

2. **선택지 UI 세부 사항**:
   - 다중 선택이 필요한 경우가 있는지
   - 선택 후 되돌리기 UX 필요?
   - 선택지 개수 제한?

3. **보드 전송 시 동작**:
   - 기존 탭에 추가 vs 새 탭 생성?
   - 차트만 전송 vs 설명 텍스트도 함께?
   - 전송 후 보드로 자동 이동?

4. **파일 업로드 방식**:
   - Composer 상시 버튼 vs AI 요청 시에만?
   - 지원 파일 형식 범위?
   - 대용량 파일 처리?

---

## Code References

### Frontend - 채팅 UI
- [response.tsx](frontend/pluto_duck_frontend/components/ai-elements/response.tsx) - Streamdown 마크다운 렌더링
- [code-block.tsx](frontend/pluto_duck_frontend/components/ai-elements/code-block.tsx) - 코드블록 렌더링
- [confirmation.tsx](frontend/pluto_duck_frontend/components/ai-elements/confirmation.tsx) - Approval UI
- [queue.tsx](frontend/pluto_duck_frontend/components/ai-elements/queue.tsx) - Todo 체크리스트

### Frontend - 채팅 렌더러
- [RenderItem.tsx](frontend/pluto_duck_frontend/components/chat/renderers/RenderItem.tsx) - 타입별 디스패처
- [ToolRenderer.tsx](frontend/pluto_duck_frontend/components/chat/renderers/ToolRenderer.tsx) - 도구 결과 렌더링
- [AssistantMessageRenderer.tsx](frontend/pluto_duck_frontend/components/chat/renderers/AssistantMessageRenderer.tsx) - AI 메시지 + Send to Board 버튼

### Frontend - 채팅 입력
- [Composer.tsx](frontend/pluto_duck_frontend/components/chat/Composer.tsx) - 입력 컴포저 (파일 업로드 없음)
- [ChatPanel.tsx](frontend/pluto_duck_frontend/components/chat/ChatPanel.tsx) - 채팅 패널

### Frontend - 보드 (시각화)
- [BoardsView.tsx](frontend/pluto_duck_frontend/components/boards/BoardsView.tsx) - 보드 메인 뷰
- [BoardEditor.tsx](frontend/pluto_duck_frontend/components/editor/BoardEditor.tsx) - Lexical 에디터
- [AssetChartView.tsx](frontend/pluto_duck_frontend/components/editor/components/AssetChartView.tsx) - **차트 렌더링 (recharts)**
- [AssetTableView.tsx](frontend/pluto_duck_frontend/components/editor/components/AssetTableView.tsx) - 테이블 렌더링

### Frontend - 타입
- [chatRenderItem.ts](frontend/pluto_duck_frontend/types/chatRenderItem.ts) - 렌더 아이템 타입
- [tool-types.ts](frontend/pluto_duck_frontend/components/ai-elements/tool-types.ts) - Tool UI 상태

### Frontend - API
- [boardsApi.ts](frontend/pluto_duck_frontend/lib/boardsApi.ts) - 보드 API

### Backend - 이벤트
- [events.py](backend/pluto_duck_backend/agent/core/events.py) - 이벤트 타입 정의

### Backend - 스킬
- [skills/](backend/pluto_duck_backend/templates/skills/) - 기존 스킬 템플릿

### 관련 계획 문서
- [001_independent_chat_ui_rendering.md](docs/plans/001_independent_chat_ui_rendering.md) - Send to Board 구현 계획 (Phase 4)
